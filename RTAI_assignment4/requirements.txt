# Python 3.11 required
# Install PyTorch with CUDA (for GPU support):
#   pip install torch==2.11.0+cu126 torchvision==0.26.0+cu126 --index-url https://download.pytorch.org/whl/cu126
# For CPU only:
#   pip install torch==2.11.0 torchvision

torch==2.11.0
torchvision==0.26.0
onnx==1.22.0
onnxruntime==1.27.0
numpy==2.4.4

# Install alpha-beta-CROWN and auto_LiRPA:
#   cd alpha-beta-CROWN
#   pip install .
#   pip install -e auto_LiRPA
